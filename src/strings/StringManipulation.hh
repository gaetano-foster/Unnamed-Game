#ifndef __STRINGMANIPULATION_H__
#define __STRINGMANIPULATION_H__
#include <string>
#include <vector>

std::vector<std::string> str_split(std::string str, std::string sep);
#endif // __STRINGMANIPULATION_H__